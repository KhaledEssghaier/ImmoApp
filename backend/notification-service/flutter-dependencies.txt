# Add to pubspec.yaml dependencies

dependencies:
  # ... existing dependencies
  
  # Firebase & Notifications
  firebase_core: ^2.24.0
  firebase_messaging: ^14.7.0
  flutter_local_notifications: ^16.2.0
  
  # For JSON encoding
  # dio is already in your dependencies
